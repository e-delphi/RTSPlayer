<?xml version="1.0" encoding="utf-8"?>
<!-- BEGIN_INCLUDE(manifest) -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="%package%"
    android:versionCode="%versionCode%"
    android:versionName="%versionName%"
    android:installLocation="%installLocation%">
    <uses-sdk android:minSdkVersion="%minSdkVersion%" android:targetSdkVersion="%targetSdkVersion%" />
<%uses-permission%>
    <uses-feature android:glEsVersion="0x00020000" android:required="true"/>
    <queries>
<%queries-child-elements%>
        <!-- Sem isto, no Android 11+ o getLaunchIntentForPackage do Tailscale
             volta nil mesmo com o app instalado: a visibilidade de pacotes é
             filtrada, e só quem está declarado aqui aparece. Necessário para a
             câmera marcada como "rede Tailscale" (ver VMS.Net.Tailscale). -->
        <package android:name="com.tailscale.ipn" />
    </queries>
    <application
        android:persistent="%persistent%"
        android:restoreAnyVersion="%restoreAnyVersion%"
        android:label="%label%"
        android:debuggable="%debuggable%"
        android:largeHeap="%largeHeap%"
        android:icon="%icon%"
        android:theme="%theme%"
        android:hardwareAccelerated="%hardwareAccelerated%"
        android:resizeableActivity="true"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true">
<%provider%>
<%application-meta-data%>
<%uses-libraries%>
        <!-- Trigger Google Play services to install the backported photo picker module. -->
        <service
            android:name="com.google.android.gms.metadata.ModuleDependencies"
            android:enabled="false"
            android:exported="false"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="com.google.android.gms.metadata.MODULE_DEPENDENCIES" />
            </intent-filter>

            <meta-data android:name="photopicker_activity:0:required" android:value="" />
        </service>
<%services%>
        <!-- Our activity is a subclass of the built-in NativeActivity framework class.
             This will take care of integrating with our NDK code. -->
        <!-- windowSoftInputMode=adjustResize: com o padrao (adjustPan) a
             janela nao encolhe quando o teclado sobe. Ele fica POR CIMA, e o
             campo que se esta digitando some atras dele. Com resize a area
             util diminui e o WebView acompanha. -->
        <!-- Tema preto, e daqui, e nao do styles.xml.

             O styles.xml e styles-v21.xml que o Delphi gera saem com
             Theme.Material.Light.NoActionBar, cujo windowBackground e #FAFAFA:
             e essa a cor que o sistema pinta na janela enquanto o processo
             sobe, antes de existir formulario, WebView ou pagina. Editar
             aqueles arquivos nao adianta, eles sao SAIDA de build e a IDE os
             reescreve a cada compilacao.

             O tema declarado na activity vence o da application, e este arquivo
             e fonte do projeto: a IDE so substitui os %placeholders% dele.
             Theme.Black.NoTitleBar da janela preta; e como ele nao e da familia
             Material, nao pinta as barras do sistema por conta propria, entao a
             de cima fica preta tambem. -->
        <activity
            android:name="com.embarcadero.firemonkey.FMXNativeActivity"
            android:exported="true"
            android:label="%activityLabel%"
            android:theme="@android:style/Theme.Black.NoTitleBar"
            android:configChanges="orientation|keyboard|keyboardHidden|screenSize|screenLayout|uiMode"
            android:windowSoftInputMode="adjustResize"
            android:launchMode="singleTask">
            <!-- Tell NativeActivity the name of our .so -->
            <meta-data android:name="android.app.lib_name" android:value="%libNameValue%" />

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
<%activity%>
<%receivers%>
    </application>
</manifest>
<!-- END_INCLUDE(manifest) -->
